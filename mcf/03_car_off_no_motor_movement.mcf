% Requirement 3
% The motors should not move when car is off


% Requirement 3a
% After a VCS(Off), inevitably a MotorForce(Static) must occur (without a VCS change?)
[true* . c_VCS(Off)] mu Y . (
	[!aMotorForce(Static)]Y && <true>true
) &&

% Requirement 3b
% In between a VCS(Off) action and a VCS(On) action, a MotorForce(Forwards) or MotorForce(Backwards) should never be seen.
[true* . c_VCS(Off) . (!(aMotorForce(Backwards) || aMotorForce(Forwards)))* . c_VCS(On)] false