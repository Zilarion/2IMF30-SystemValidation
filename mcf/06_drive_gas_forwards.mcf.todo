% Requirement 6
% After a DNRP(D) action occured after which a GasPedal(Pressed) occured there should be a path in which MotorForce(Forwards) occurs in a finite amount of steps.
%TODO : FINISH

% After we see a DNRP set to drive
[true* . aDNRP(D)]
	% If there is a way how we can do a motor force
	mu X.(
		[!aMotorForce(Forwards)] X || <aMotorForce(Forwards)>true
	)